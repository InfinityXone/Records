apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: infinity-x-one
spec:
  selector:
    app: agent-core
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP